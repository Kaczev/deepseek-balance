// 图标资源（设计 §10.3、F2b/F4）。
//
// 这里把仓库里的 icon.png 作为**二进制资源**打进 exe。理由（规格要求说清选了哪种）：
//   · 选 RCDATA + 运行期解码，而不是预生成多尺寸 .ico：
//     ① 仓库里那份 PNG 就是图标的唯一来源，多一个 .ico 就是第二个真相（改了 PNG
//        忘了重新生成 .ico 是这类资源最经典的事故，而症状只有"图标旧了"）；
//     ② 各档尺寸在运行期按**当前 DPI**算（16/20/24/32），168 DPI 那种非整数档也覆盖得到，
//        固定的一堆 .ico 档位总会有一档对不上，对不上就是系统拉伸 = 模糊；
//     ③ 解码器（WIC）本来就在链接表里（renderer.cpp 导出 PNG 用的就是它），
//        为零新增依赖。代价是 exe 大约大 1 MB，对单文件分发可以接受。
//
// ★ 运行期不读任何外部文件：资源进了 exe 之后，icon.png 只在这台机器上编译时才需要。
// ★ FindResourceW 是按**字符串名**找的，所以下面必须带引号 —— 不带引号是整数 101，
//   两边就对不上了（症状是"图标资源读不到"）。
// ★ 路径是相对**本文件所在目录**（rc.exe 的包含搜索以 .rc 自己为基准），所以是
//   "..\\icon.png"：仓库根目录那一份。写成 "icon.png" 会去 src\ 里找，报
//   RC2135 file not found（实测踩过）。
DSHB_ICON_PNG RCDATA "..\\icon.png"
