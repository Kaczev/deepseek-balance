// exe 的资源（设计 §10.3、F2b/F4）。
//
// ===========================================================================
// ★★ 一个 exe 有**两个**"图标"，是两套完全不同的机制（2026-09-19 踩过）
// ===========================================================================
//   · 下面那条 **RCDATA** 是**托盘图标**的来源：运行期由 WIC 解码 PNG、按当前 DPI
//     生成 16/20/24/32 各档。它一直是对的。
//   · 下面那条 **ICON** 是 **shell 显示 exe 文件本身**用的（资源管理器 / 桌面 /
//     任务栏）。它只能由编译期决定，**没有它 exe 就是 Windows 默认的空白程序图标**。
//   症状：托盘里的图标好好的，可桌面上那个 exe 看着"没上图标"。
//   改一个不会影响另一个 —— 这是两个独立的资源。
//
// ★ 为什么 RCDATA 用运行期解码、而文件图标用预生成的 .ico：
//   文件图标**根本不由我们的代码画** —— 是 explorer 从 PE 的 .rsrc 里读 ICON 资源，
//   所以它必须在**编译期**就在。反过来，托盘图标是我们自己解码的，所以那一段
//   "仓库里那份 PNG 是唯一真相、别多一个会过期的 .ico"的论证只适用于托盘那一路；
//   对文件图标而言，.ico 不是"第二个真相"，它是**另一件东西**。
//   代价：改了 icon.png 之后，托盘图标自动跟着变，**文件图标要重新生成 .ico**。
//
// ---------------------------------------------------------------------------
// 托盘图标：仓库根的 icon.png 原样打进去
// ---------------------------------------------------------------------------
// ★ FindResourceW 是按**字符串名**找的，所以下面必须带引号 —— 不带引号是整数 101，
//   两边就对不上了（症状是"图标资源读不到"）。
// ★ 路径是相对**本文件所在目录**（rc.exe 的包含搜索以 .rc 自己为基准），所以是
//   "..\\icon.png"：仓库根目录那一份。写成 "icon.png" 会去 src\ 里找，报
//   RC2135 file not found（实测踩过）。
DSHB_ICON_PNG RCDATA "..\\icon.png"

// ---------------------------------------------------------------------------
// 文件图标：预生成的多尺寸 .ico（由 icon.png 生成，9 档：16..256）
// ---------------------------------------------------------------------------
// ★ 编号用 1：explorer 取**编号最小**的那个图标当作文件图标。同一个 .ico 里装了多档，
//   由 shell 按要显示的大小自己挑（16 列表视图 / 32 中图标 / 256 大图标/缩略图）。
1 ICON "dshb.ico"

// ---------------------------------------------------------------------------
// 版本信息（鼠标悬停在 exe 上、或文件属性 -> 详细信息 里看到的就是这些）
// ---------------------------------------------------------------------------
// ★ 以前没有这一段，于是"版本号"只活在提交信息与 zip 文件名里 —— 收到 zip 的人
//   无法从 exe 本身判断自己拿的是哪一版。
// ★ 全部值都用 ASCII：rc.exe **不认** `\u` 转义（写了会变成字面量 `\u4f59...`，
//   实测踩过），而字符串块的长度必须是 8 的倍数，所以每块末尾留 \0 填充。
// ★ 一个版本号活在**五处**，发版时全都要动（漏一处就会出现"包里的 exe 说自己
//   是上一版"）：
//     1. 本文件下面的 FILEVERSION / PRODUCTVERSION，以及 StringFileInfo 里那两条
//        FileVersion / ProductVersion —— 四段式，exe 属性里显示的就是它们；
//     2. http_get.cpp 里的 User-Agent 默认值 —— 三段式，服务器看到的是它；
//     3. dist\README.md 的标题 —— 拿到 zip 的人第一眼看到的；
//     4. dist 里 zip 的文件名；
//     5. src\app.manifest 的 assemblyIdentity version —— 四段式。清单是链接期进的
//        exe，所以它坏了会**报链接/构建错**，不会静默不一致（0.2.4 起才开始跟着走，
//        在此之前它一直停在 0.2.0.0）。
//   （这五处的格式天然不同：资源与清单是四段的 0,2,4,0，User-Agent 惯例是三段的
//   0.2.4，所以这是**五处手工同步**，没有单一真相 —— 核版本就用下面的文件属性。）
// ★★ 类型的编号必须是数字 `1`，**不许写回 `VS_VERSION_INFO`**（0.2.4 实测）。
//   `VS_VERSION_INFO` 是 winver.h 里的宏，而本文件**一个 #include 都没有**（理由：
//   全 ASCII、不依赖 SDK 头，见上）；未定义的标识符会被 rc.exe 当成**名字串**，
//   于是资源类型不是 16、ID 不是 1 —— 编译链接都正常，只是版本资源**永远读不出来**：
//   GetFileVersionInfoSizeW 返回 0、lastErr=1813 ERROR_RESOURCE_TYPE_NOT_FOUND，
//   exe 属性里 FileVersion/ProductVersion 全空、FileVersionRaw 显示 0.0.0.0，
//   而它在 0.2.0~0.2.3 一直是这样、一直没人发现（版本文本**确实**在镜像里，
//   所以字节级 grep 是搜得到的，只有 Windows 的读取路径看不见 —— 这就是它藏了这么久的原因）。
//   → 改用数字 `1` 后（另一条等效的路是补 `#include <winver.h>`，但那要给这个
//     刻意不依赖 SDK 头的文件引一个头，没必要）实测：GetFileVersionInfoSizeW = 1380、
//     lastErr = 0，属性读回 FileVersion = ProductVersion = "0.2.4.0"、
//     dwFileVersionMS/LS = 0.2 / 4.0。
//   → 发版时**照旧核一遍文件属性**（悬停 exe 或 属性→详细信息）；若哪天它又变空，
//     先看的就是这一行还在不在。
1 VERSIONINFO
 FILEVERSION    0,2,4,0
 PRODUCTVERSION 0,2,4,0
 FILEOS         0x40004L
 FILETYPE       0x1L
BEGIN
    BLOCK "StringFileInfo"
    BEGIN
        BLOCK "040904b0"
        BEGIN
            VALUE "CompanyName",      "Kaczev\0"
            VALUE "FileDescription",  "DeepSeek balance widget\0"
            VALUE "FileVersion",      "0.2.4.0\0"
            VALUE "InternalName",     "dshb\0"
            VALUE "LegalCopyright",   "MIT License\0"
            VALUE "OriginalFilename", "dshb.exe\0"
            VALUE "ProductName",      "deepseek-balance\0"
            VALUE "ProductVersion",   "0.2.4.0\0"
        END
    END
    BLOCK "VarFileInfo"
    BEGIN
        VALUE "Translation", 0x409, 1200
    END
END
