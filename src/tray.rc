// exe 的资源（设计 §10.3、F2b/F4）。
//
// ===========================================================================
// ★★ 一个 exe 有**两个**"图标"，是两套完全不同的机制（2026-09-19 踩过）
// ===========================================================================
//   · 下面那条 **RCDATA** 是**托盘图标**的来源：运行期由 WIC 解码 PNG、按当前 DPI
//     生成 16/20/24/32 各档。它一直是对的。
//   · 下面那条 **ICON** 是 **shell 显示 exe 文件本身**用的（资源管理器 / 桌面 /
//     任务栏）。它只能由编译期决定，**没有它 exe 就是 Windows 默认的空白程序图标**。
//   症状：托盘里的图标好好的，可桌面上那个 exe 看着"没上图标"。
//   改一个不会影响另一个 —— 这是两个独立的资源。
//
// ★ 为什么 RCDATA 用运行期解码、而文件图标用预生成的 .ico：
//   文件图标**根本不由我们的代码画** —— 是 explorer 从 PE 的 .rsrc 里读 ICON 资源，
//   所以它必须在**编译期**就在。反过来，托盘图标是我们自己解码的，所以那一段
//   "仓库里那份 PNG 是唯一真相、别多一个会过期的 .ico"的论证只适用于托盘那一路；
//   对文件图标而言，.ico 不是"第二个真相"，它是**另一件东西**。
//   代价：改了 icon.png 之后，托盘图标自动跟着变，**文件图标要重新生成 .ico**。
//
// ---------------------------------------------------------------------------
// 托盘图标：仓库根的 icon.png 原样打进去
// ---------------------------------------------------------------------------
// ★ FindResourceW 是按**字符串名**找的，所以下面必须带引号 —— 不带引号是整数 101，
//   两边就对不上了（症状是"图标资源读不到"）。
// ★ 路径是相对**本文件所在目录**（rc.exe 的包含搜索以 .rc 自己为基准），所以是
//   "..\\icon.png"：仓库根目录那一份。写成 "icon.png" 会去 src\ 里找，报
//   RC2135 file not found（实测踩过）。
DSHB_ICON_PNG RCDATA "..\\icon.png"

// ---------------------------------------------------------------------------
// 文件图标：预生成的多尺寸 .ico（由 icon.png 生成，9 档：16..256）
// ---------------------------------------------------------------------------
// ★ 编号用 1：explorer 取**编号最小**的那个图标当作文件图标。同一个 .ico 里装了多档，
//   由 shell 按要显示的大小自己挑（16 列表视图 / 32 中图标 / 256 大图标/缩略图）。
1 ICON "dshb.ico"

// ---------------------------------------------------------------------------
// 版本信息（鼠标悬停在 exe 上、或文件属性 -> 详细信息 里看到的就是这些）
// ---------------------------------------------------------------------------
// ★ 以前没有这一段，于是"版本号"只活在提交信息与 zip 文件名里 —— 收到 zip 的人
//   无法从 exe 本身判断自己拿的是哪一版。
// ★ 全部值都用 ASCII：rc.exe **不认** `\u` 转义（写了会变成字面量 `\u4f59...`，
//   实测踩过），而字符串块的长度必须是 8 的倍数，所以每块末尾留 \0 填充。
// ★ 这里的版本号要与 http_get.cpp 的 User-Agent 和 zip 文件名一致，改一处要改三处。
VS_VERSION_INFO VERSIONINFO
 FILEVERSION    0,2,1,0
 PRODUCTVERSION 0,2,1,0
 FILEOS         0x40004L
 FILETYPE       0x1L
BEGIN
    BLOCK "StringFileInfo"
    BEGIN
        BLOCK "040904b0"
        BEGIN
            VALUE "CompanyName",      "Kaczev\0"
            VALUE "FileDescription",  "DeepSeek balance widget\0"
            VALUE "FileVersion",      "0.2.1.0\0"
            VALUE "InternalName",     "dshb\0"
            VALUE "LegalCopyright",   "MIT License\0"
            VALUE "OriginalFilename", "dshb.exe\0"
            VALUE "ProductName",      "deepseek-balance\0"
            VALUE "ProductVersion",   "0.2.1.0\0"
        END
    END
    BLOCK "VarFileInfo"
    BEGIN
        VALUE "Translation", 0x409, 1200
    END
END
